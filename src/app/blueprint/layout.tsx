export const metadata = {
  title: 'Blueprint',
  description: 'Blueprint for website',
};

export default function CalculatorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
