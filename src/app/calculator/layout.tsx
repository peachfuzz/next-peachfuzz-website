export const metadata = {
  title: 'Calculator',
  description: 'Euclidean Calculator',
};

export default function CalculatorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
